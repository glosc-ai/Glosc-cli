# Minimal pyproject.toml (adjust as needed)

[project]
name = "test-tools"
version = "0.1.0"
description = ""
authors = [{ name = "Your Name" }]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
